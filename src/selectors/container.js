export const TYPE_SHELF_CONTAINER = 'ShelfContainer"';

export const getType = (container) => container?.type;
export const getStyle = (container) => container.style;
export const getSet = (container) => container?.set;
